apiVersion: v1
kind: Service
metadata:
  name: {{ include "order-query-service.fullname" . }}
  labels:
    {{- include "order-query-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "order-query-service.selectorLabels" . | nindent 4 }}
